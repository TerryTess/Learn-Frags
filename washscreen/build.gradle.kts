// Top-level build file where you can add configuration options common to all sub-projects/modules.
plugins {
    id ("com.android.application") version "1.9.0" apply false// Apply Android plugin directly
    id ("org.jetbrains.kotlin.android")version "1.9.0-1.0.13" apply false // Apply Kotlin plugin directly
}
