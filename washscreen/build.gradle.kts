// Top-level build file where you can add configuration options common to all sub-projects/modules.
plugins {
    id 'com.android.application' // Apply Android plugin directly
    id 'org.jetbrains.kotlin.android' // Apply Kotlin plugin directly
}
